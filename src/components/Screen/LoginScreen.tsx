import { PageTemplate } from "@_src/templates/PageTemplate";
import Login from "../Login";

export const LoginScreen = () => {
  return (
    <PageTemplate>
      <Login />
    </PageTemplate>
  );
};
